# Full Run Receipt — 2026-09-24

Branch: `reality-kernel-integration-2026-09-24`
Latest tested commit: `105693489ea6113bee9a46e1c2da1eb057b6ad7b`

## Local execution

Environment:
- Node: 22.16.0
- TypeScript: 5.8.3
- Python: 3.13.5

Economic/Reality core typecheck:
- `tsc -p tsconfig.json`
- RESULT: PASS

Core runtime smoke:
- Executed economic position separation.
- Executed evidence-gated drift check.
- Executed Reality Gate receipt projection.
- Executed malformed-entry validation.
- RESULT: PASS

Observed smoke receipt:

```
FULL LOCAL CORE RUN: PASS
```

## GitHub Actions

The repository accepted the pull-request workflows, but the GitHub job records exposed zero executed steps for the failing jobs. The connected GitHub interface does not expose their runner logs or annotations, so the failure cause cannot be claimed from those runs.

The newly added `EVEZ full-run integrity` workflow also reached both jobs and both jobs completed with failure while exposing zero steps.

This is recorded as:

```
CI FAILURE OBSERVED
CAUSE UNKNOWN
```

It is not promoted into a code defect without executable evidence.

## External deployment status

The commit received Vercel GitHub statuses reporting:
- deployment canceled from the Vercel Dashboard
- account is blocked
- deployment account is blocked

No successful Vercel deployment is claimed.

A working Netlify project was not found through the connected Netlify account lookup, so no new Netlify site was created implicitly.

## Merge state

- Autonomous Ledger PR #91: open; mergeable status observed, but protected review/merge requirements remain.
- AgentNet PR #94: open.
- EVEZ-OS PR #83: open.

Self-approval was rejected by GitHub, and a direct merge attempt was rejected because at least one independent approving review is required.

## Invariants preserved

```
CLAIM != EVIDENCE
NARRATIVE != FACT
CAPABILITY != AUTHORITY
FAILURE != INTENT
SILENCE != SUPPRESSION
DECLARED != EFFECTIVE
CLAIMED VALUE != REALIZED VALUE
```
