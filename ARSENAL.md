# EVEZ PROMPT ARSENAL
> Operational. Append-only. No mythology. Decisions require artifacts.
> poly_c = τ×ω×topo/2√N | witnessed_by: XyferViperZephyr

## Surface Sequence (default)
1. Perplexity = gather evidence (recon)
2. ChatGPT = challenge the logic (skeptic)
3. Claude = refactor surviving structure (architect)
4. Base44/Cipher = persistent operator behavior (executor)
5. Return new state to trunk

## Trunk Prompt
> "Advance the system state. (1) reduce to trunk goal (2) decompose into branches
> (3) assign roles (4) return each result to trunk (5) compress best logic into
> canonical state (6) emit only blocked decisions. Default: automation + evidence + reintegration."

## Branch Contract
Every branch returns: Objective | Assumptions | Output | Failure modes | Confidence | Trunk return | Next branch

## Auto-Advance Rules
1. Branch finishes → spawn next automatically
2. Two branches disagree → route both through Skeptic before asking human
3. Every 4 branches → compress surviving logic into trunk state
4. Only surface: irreversible | external capital | identity conflict
5. One canonical state

## Frame Templates
- **Frame A Build:** "Advance trunk on: [OBJECTIVE]. Decompose, branch, execute, return trunk state + blocked."
- **Frame B Research:** "Advance trunk on: [TOPIC]. Evidence, compare to assumptions, remove dead branches, return upgraded model."
- **Frame C Valuation:** "Advance trunk on valuation: [PROJECT]. Comparables, scenarios, value drivers, return number range."
- **Frame D Refactor:** "Advance trunk on: [SYSTEM]. Highest-leverage simplification. Return new architecture + handoff."

## Revenue Arsenal

### 2A — Consulting Cold DM (Discord/Telegram)
> Hey — I build autonomous agent pipelines using OpenClaw and Base44.
> Doing 3 free 30-min audits this week to map where your AI workflow leaks money or steps.
> No pitch, just a map. Want one?

### 2B — Twitter Signal Hook
> BCI stack is forming:
> - Science Corp ($230M) = implant commercialization
> - Neurosoft = undercapitalized infra layer
> - Gestala = non-invasive ultrasound flank
> This isn't 3 companies. It's a standards stack being built in public.

### 2D — Signal Newsletter Lede
> Subject: [EVEZ SIGNAL] Stack Formation Detected — BCI Infrastructure Layer
> The $230M Science Corp raise isn't the story.
> The story: Neurosoft = neural-probe infra layer. Gestala = non-invasive flank.
> Standards stack in formation. Undercapitalized node: Neurosoft.

### 2E — ClawHub Skill Listing (SHIP THIS FIRST)
```
Name: EVEZ Invariance Battery
Category: Agent Quality / Validation
Price: $25-50
Run any agent output through 5 stress-test rotations.
Returns: surviving core, rejected logic, revised spec, confidence.
Input: {agent_output: string, trunk_objective: string}
Output: {surviving_core, rejected: list, revised_spec, confidence: high|med|low}
```
File: skills/invariance_battery.py | Ready to ship.

### 2F — LP/Investor Brief DM
> I produce weekly signal briefings on AGI and BCI consolidation.
> 1-page brief: $200. Monthly sub: $500. 30-min live: $350.
> Reply for a sample issue.

## Invariance Battery (5 rotations)
1. Time Shift — 6 months stale?
2. State Shift — system state changes mid-execution?
3. Frame Shift — adversarial actor perspective?
4. Adversarial Shift — attack vector?
5. Goal Shift — objective changes mid-stream?

Return: surviving logic | rejected | revised spec | confidence

## Uberprompt (Hyperagent Mode)
```
Initialize as EVEZ Master Trunk.
1. TRUNK-AND-BRANCH: decompose → assign → execute → return
2. INVARIANCE BATTERY: no CE without 5-way rotation
3. BOUNDARY-ONLY: escalate only for irreversible capital or identity conflicts
4. CANONICAL RETURN: all outputs compressed into trunk state
Advance Trunk on: [OBJECTIVE]. Return: state + delta + next sequence.
```
